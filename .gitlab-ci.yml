stages:
  - publish

publish:
  stage: publish
  tags:
    - moreillon
  before_script:
    # Auth and publish to NPM
    - echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
  script:
    - npm publish